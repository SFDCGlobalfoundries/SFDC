/*
Author: Vijay Mahajan
Company: Cognizant
Description: It is a utility class, that defines a set of methods that perform common, often re-used functions.
             
History:
<Vijay>     04032014 - class created
<Vijay>		20032014 - removed with sharing keyword from class declaration
*/
public class QS_CommonUtil{
    
    /*
    public static List<User> getUsers(Set<Id> accountIdset){
        List<User> userList = new List<User>();
         if(accountIdSet != null && !accountIdSet.isEmpty()){
           userList =   [SELECT     Id, Contact.AccountId 
                         FROM       User 
                         WHERE      IsActive = true and Contact.AccountId 
                         IN         : accountIdSet
                         ]; 
        }
        return userList;
    }
    */
}